name = "matemados-m2"
compatibility_date = "2026-07-01"
pages_build_output_dir = "."

# Variables no sensibles. También pueden configurarse en
# Workers & Pages → proyecto → Settings → Environment variables.
[vars]
FIREBASE_PROJECT_ID = "REEMPLAZAR_FIREBASE_PROJECT_ID"
ADMIN_EMAILS = "vicentebasoalto2@gmail.com"
SUPABASE_URL = "https://nurqpfpatxxewcetzdvt.supabase.co"
OPENAI_MODEL = "gpt-5.6-luna"

# Configura estas claves como secretos, nunca dentro del repositorio:
# npx wrangler pages secret put SUPABASE_SERVICE_ROLE_KEY
# npx wrangler pages secret put OPENAI_API_KEY
